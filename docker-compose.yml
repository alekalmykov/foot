services:
  bot:
    build: .
    container_name: football-poll-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.db
    volumes:
      - ./data:/data
